import {StyleSheet} from 'react-native'

export default StyleSheet.create({
    grande:{
        fontSize: 30,
        textAlign: 'center'
    },
    App: {
        backgroundColor: '#fff',
        flexGrow: 1,
        justifyContent: 'center',
        alignItems: 'center',
        padding: 20
    }
})